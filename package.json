{
  "name": "testpilot-ai-playwright",
  "version": "0.1.0",
  "description": "TestPilot AI — React E2E runner",
  "scripts": {
    "test:react": "node scripts/playwright_runner.js",
    "setup": "npx playwright install chromium"
  },
  "dependencies": {
    "js-yaml": "^4.1.0",
    "playwright": "^1.42.0"
  }
}
